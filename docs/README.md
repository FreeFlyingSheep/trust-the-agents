# Notes | 说明

This is not the project's documentation folder.
It is used for GitHub Pages deployment and contains exported Web build artifacts (static files).

这个目录不是项目文档目录。
它用于 GitHub Pages 部署，存放导出的 Web 项目构建产物（静态文件）。
